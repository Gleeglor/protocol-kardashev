

## Compiling flags
If not specified → defaults to current machine + Debug
`zig build -Dtarget=... -Doptimize=ReleaseFast`

-Dtarget=[native, x86_64-windows, wasm32, arm-linux]
-Doptimize=[Debug, ReleaseSafe, ReleaseFast, ReleaseSmall]
